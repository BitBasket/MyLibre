/vendor/
/.env
/data/*.json
/data/*.csv
/data/*.sqlite
/data/*.sqlite-*
/data/keys/
/data/*.asc
/public/*.asc
/public/b/
# Cleartext snapshot names are never produced any more; ignore them anyway so a
# leftover from an older deployment can never be committed.
/public/current.json
/public/status.json
/public/history.json
/public/history-*.json
# Plaintext history exports / legacy inputs (PHI). .dockerignore lists these too.
/mylibre.history.json
/history.json
/coverage/
/.phpunit.cache
/phpunit.result.cache
